<UserControl x:Class="SophiaWin11.App.Views.TweakRowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:SophiaWin11.App.Converters"
             Margin="0,0,0,8">

    <ui:CardControl>
        <ui:CardControl.Icon>
            <ui:SymbolIcon Symbol="Wrench24" />
        </ui:CardControl.Icon>
        <ui:CardControl.Header>
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Name}"
                           FontFamily="{DynamicResource FontFamilyBody}"
                           FontWeight="SemiBold"
                           FontSize="{DynamicResource FontSizeBody}"
                           TextWrapping="Wrap" />
                <TextBlock Text="{Binding Description}"
                           FontFamily="{DynamicResource FontFamilyBody}"
                           Foreground="{DynamicResource BrushTextSecondary}"
                           FontSize="{DynamicResource FontSizeCaption}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0" />
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <Border Background="{Binding RiskLevel, Converter={StaticResource RiskLevelToBrushConverter}}"
                            CornerRadius="{DynamicResource RadiusCard}"
                            Padding="6,2">
                        <TextBlock Text="{Binding RiskLevel}"
                                   FontSize="{DynamicResource FontSizeCaption}"
                                   Foreground="{DynamicResource BrushTextOnAccent}" />
                    </Border>
                    <Border Background="{DynamicResource BrushAccentPeacock}"
                            CornerRadius="{DynamicResource RadiusCard}"
                            Padding="6,2"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Visibility="{Binding RequiresRestart, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Restart required"
                                   FontFamily="{DynamicResource FontFamilyBody}"
                                   FontSize="{DynamicResource FontSizeCaption}"
                                   Foreground="{DynamicResource BrushTextOnAccent}" />
                    </Border>
                </StackPanel>
            </StackPanel>
        </ui:CardControl.Header>

        <Grid>
            <ui:ProgressRing IsIndeterminate="True"
                              Width="20"
                              Height="20"
                              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <StackPanel Orientation="Horizontal"
                        Visibility="{Binding IsBusy, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <ui:Button Content="Apply"
                           Appearance="Primary"
                           Command="{Binding ApplyCommand}"
                           Visibility="{Binding IsApplied, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                <ui:Button Content="Revert"
                           Margin="8,0,0,0"
                           Command="{Binding RevertCommand}"
                           Visibility="{Binding IsApplied, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Grid>
    </ui:CardControl>
</UserControl>

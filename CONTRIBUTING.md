# Contributing

This project targets Windows 11 exclusively and follows strict conventions:

- No comments in source code
- Every change reflected in `CHANGELOG.md`
- Strict 4-segment SemVer (`MAJOR.MINOR.PATCH.BUILD`)
- Pull requests must build with zero warnings on `dotnet build -c Release`
